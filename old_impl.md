"# ManifestGuard ù Implementation Details Reference\n\n> Everything that's actually in the code right now ù every weight, threshold, and decision.\n\n---\n\n## 1. Power Score ù How It's Calculated\n\nThe power score measures **how much browser and data access** the extension has. It's a simple weighted sum of the extension's granted permissions, capped at 100.\n\n### Permission Weight Table\n\n| Permission | Points | Why This Weight |\n|------------|--------|-----------------|\n| `<all_urls>` | 40 | Full access to every website the user visits. The most powerful single permission |\n| `*://*/*` | 40 | Same as above, just a different syntax Chrome accepts |\n| `webRequestBlocking` | 30 | Can intercept, modify, or block ANY network request before it reaches the server. Used by ad blockers, but also by man-in-the-middle extensions |\n| `debugger` | 30 | Gives programmatic access to Chrome DevTools ù can inspect, modify, and control any tab. Essentially full control over the browser |\n| `proxy` | 25 | Can route all browser traffic through a custom proxy server. Used by VPNs, but a malicious extension could route traffic through an attacker's server |\n| `cookies` | 20 | Read/write cookies across any domain (if combined with host permissions). Cookies contain session tokens ù stealing them = account hijacking |\n| `webRequest` | 18 | Can observe all network requests (but not block them without `webRequestBlocking`). Gives visibility into every URL the user visits |\n| `declarativeNetRequest` | 15 | Manifest V3 replacement for webRequestBlocking. Can modify requests using declarative rules, but Google limits what it can do compared to V2 |\n| `history` | 15 | Full access to browsing history ù every URL the user has ever visited |\n| `management` | 15 | Can see all other installed extensions, enable/disable them, or uninstall them. Could be used to disable security extensions |\n| `tabs` | 12 | Can see the URL and title of every open tab. Reveals what the user is doing right now |\n| `activeTab` | 10 | Can 
<truncated 20769 bytes>
"# Technical Implementation Details\n\nManifestGuard relies on a modern, decoupled architecture designed for scalability, security, and lightning-fast AI inference.\n\n## Technology Stack\n\n### Backend (Render)\n- **Framework**: FastAPI (Python 3)\n- **Data Models**: Pydantic\n- **AI Integration**: Groq API SDK (using Llama-3/Mixtral models for ultra-low latency inference)\n- **Scraping**: `urllib` with regex parsing for Chrome Web Store live reputation metrics\n- **Deployment**: Render Web Service\n\n### Frontend (Vercel)\n- **Framework**: React + Vite\n- **Styling**: Vanilla CSS (Custom Glassmorphism Design System)\n- **State Management**: React Hooks\n- **Icons**: Lucide React\n- **Deployment**: Vercel\n\n### Client Integration (Chrome)\n- **Format**: Manifest V3 Chrome Extension\n- **APIs Used**: `chrome.management`, `chrome.storage.local`, `chrome.runtime.sendMessage`\n\n## Core Modules (`backend/`)\n\n### 1. `scanner.py`\nThe heart of the application. Contains the heuristics engine.\n- Calculates the `power_score` using a predefined dictionary of permission weights.\n- Implements `compute_suspicion_score` by evaluating requested permissions against V3 behavioral signatures.\n- Implements `choose_verdict` to intelligently bucket extensions, preventing alert fatigue by honoring the community allowlist and reputation multipliers.\n\n### 2. `reputation.py`\nThe live verification engine.\n- Dynamically scrapes `https://chrome.google.com/webstore/detail/...` to fetch user counts, star ratings, review counts, and developer badges.\n- Calculates an overarching reputation score (0-100).\n- Exposes `compute_reputation_adjustment()`, which applies mathematical dampening multipliers (e.g., `0.35x` for highly trusted extensions) to raw suspicion scores to eliminate false positives on popular tools.\n\n### 3. `ai.py`\nThe intelligence layer.\n- Uses `Groq` for sub-second inference.\n- Implements strict system prompts to force the LLM to return valid JSON responses.\n- Generates business-focused risk summaries, translate
<truncated 741 bytes>
